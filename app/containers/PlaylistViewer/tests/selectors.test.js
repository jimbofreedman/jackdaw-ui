// import { fromJS } from 'immutable';
// import { makeSelectPlaylistViewerDomain } from '../selectors';

// const selector = makeSelectPlaylistViewerDomain();

describe('makeSelectPlaylistViewerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
