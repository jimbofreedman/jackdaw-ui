// import { fromJS } from 'immutable';
// import { makeSelectMusicDomain } from '../selectors';

// const selector = makeSelectMusicDomain();

describe('makeSelectMusicDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
