// import { fromJS } from 'immutable';
// import { makeSelectMusicControlDomain } from '../selectors';

// const selector = makeSelectMusicControlDomain();

describe('makeSelectMusicControlDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
